<% unless !signed_in? %>
<div class="nav nav-tabs edit_link">
   <% if params[:folder] == 'inbox' %>
      <li class="active">
   <% else %>
      <li>
   <% end %>
	<%= link_to "Inbox", mailbox_path(:folder => 'inbox') %>
	</li>
	
	<% if params[:folder] == "sent" %>
	   <li class="active">
	<% else %>
	   <li>
	<% end %>
	<%= link_to "Sent", mailbox_path(:folder => 'sent') %>
	</li>
	      
</div>
<br>
<% end %>