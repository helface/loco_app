<% provide(:title, 'become host') %>
<div class="filler"></div>

<%= render "shared/your_header", id: current_user.id, text: "Become a host" %>
<br/>
<div class="container" id="white">

<div class="span10 offset1">
   
   <div class="row">
      <%= render 'shared/edit_tabs'%>
   </div>

   <br>
   <%= render 'form_become_host' %>
</div>
</div>

